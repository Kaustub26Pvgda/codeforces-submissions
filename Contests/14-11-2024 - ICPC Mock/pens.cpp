#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    int t;
    int n, k;
    string s;
    cin >> t;
    while (t--)
    {
        cin >> n >> k;
        cin >> s;
        string arr[k];
        for (int i{}; i < k; i++)
            cin >> arr[i];

        
    }

    return 0;
}