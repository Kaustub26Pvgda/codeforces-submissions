#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    int t;
    cin >> t;
    ll n;
    while (t--)
    {
        cin >> n;
        cout << 1 + (n + 1) * (n + 1) << endl;
    }

    return 0;
}